using System;
using Microsoft.VisualStudio.TestTools.UnitTesting;
using EquationEditor;
using System.Diagnostics;
using System.Collections.Generic;

namespace EditorTester {
    [TestClass]
    public class UnitTest1 {
        [TestMethod]
        public void TestMethod1() {
            //ParseTree tree = new ParseTree();
            //string input = @"3 + 4 * 2 / ( 1 - 5 ) ^ 2 ^ 3";
            //var result = tree.TokenQueue(input);
            //tree.BuildTree(result);
            //List<string> target = new List<string>() { "3", "4", "2", "*", "1", "5", "-", "2", "3", "^", "^", "/", "+" };
            //for(int i=0;i < target.Count; i++){
            //    Assert.IsTrue(result.Dequeue().Value == target[i]);
            //}

        }
    }
}
